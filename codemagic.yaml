workflows:
  run_benchmarks:
    name: Run benchmarks
    environment:
      xcode: 14.3
    triggering:
      events:
        - push
    scripts:
      - name: Run benchmark tests
        script: sh benchmark.sh
